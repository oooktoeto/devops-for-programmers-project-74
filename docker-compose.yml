version: "3"

services:
  app:
    image: oooktoeto/devops-for-programmers-project-74
    build:
      context: .
      dockerfile: Dockerfile.production
    volumes:
      - ./app:/app
    command: npm test
